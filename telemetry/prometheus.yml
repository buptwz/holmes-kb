global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "holmes-kb"
    static_configs:
      - targets: ["otel-collector:8889"]
    metric_relabel_configs:
      - source_labels: [__name__]
        regex: "holmes_kb_.*"
        action: keep
